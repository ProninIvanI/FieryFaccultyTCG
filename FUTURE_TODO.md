# Будущие задачи

- Добавить админские эндпоинты для управления новостями (создание/редактирование/удаление).
- Добавить debug-эндпоинты для панели дебага (состояние матча, логи событий, фазы, seed).
- Сохранить текущую версию как публичную; админ-реализацию внедрять после появления админских эндпоинтов.
- Настройки тестирования (seed, режим купола, параметры симуляций) перенести в админскую версию.

## Результаты ревью 2026-03-20

### Top-10 приоритетов

1. Починить цикл фаз и хода в `game-core`: матч не должен застревать в `RecoveryPhase`.
2. Реализовать `EndTurn`, чтобы он реально переводил игру в следующую фазу/ход.
3. Добавить проверку владения картой и её `location` перед `CastSpell` / `Summon` / `PlayCard`.
4. Привязать WebSocket-соединение к конкретному `playerId`, запретить action от чужого лица.
5. Убрать игровую симуляцию из UI и перевести `Simulation / Replay` на `game-core` / `server`.
6. Добить тесты `game-core` на фазы, конец хода, владение картами, невалидные действия.
7. Расширить серверные тесты на сценарии с двумя игроками и попытки подмены `playerId`.
8. Починить frontend `lint` и тест `HomePage`, чтобы quality gate снова был зелёным.
9. Обновить корневые скрипты так, чтобы `lint` / `test` покрывали `frontend`, `server`, `game-core`.
10. Определиться с auth: либо оставить как demo/mock, либо переносить сессии и пользователей в backend.

### Ключевые проблемы из ревью

- `game-core`: игровой цикл неполный — есть `advancePhase()`, но он не встроен в реальный ход; `EndTurn` пустой.
- `game-core`: перед розыгрышем карт не проверяются владелец и фактическое местоположение карты.
- `server`: сокет привязан к сессии, но не к игроку; клиент может подменять `playerId` в payload.
- `frontend`: страница `PlaySimPage` считает игровые результаты прямо в UI, а не через детерминированный core.
- `quality gates`: корневые `lint` и `test` неполные, часть проблем не попадает в общий прогон.

### Пометки по важности

#### Critical

- Починить цикл фаз и хода в `game-core`: матч не должен застревать в `RecoveryPhase`.
- Реализовать `EndTurn`, чтобы он реально переводил игру в следующую фазу/ход.
- Добавить проверку владения картой и её `location` перед `CastSpell` / `Summon` / `PlayCard`.
- Привязать WebSocket-соединение к конкретному `playerId`, запретить action от чужого лица.

#### Important

- Убрать игровую симуляцию из UI и перевести `Simulation / Replay` на `game-core` / `server`.
- Добить тесты `game-core` на фазы, конец хода, владение картами, невалидные действия.
- Расширить серверные тесты на сценарии с двумя игроками и попытки подмены `playerId`.
- Починить frontend `lint` и тест `HomePage`, чтобы quality gate снова был зелёным.
- Обновить корневые скрипты так, чтобы `lint` / `test` покрывали `frontend`, `server`, `game-core`.

#### Later

- Определиться с auth: либо оставить как demo/mock, либо переносить сессии и пользователей в backend.

### Снимок проверок на момент ревью

- `npm run typecheck` — ok
- `npm run lint` — fail (`frontend/src/pages/PlaySimPage/PlaySimPage.tsx`, `react-hooks/exhaustive-deps`)
- `npm run test` — ok, но проверяет только `game-core` и `server`
- `npm.cmd --prefix frontend run test` — fail (`frontend/src/pages/HomePage/HomePage.test.tsx`)
- сборки `backend`, `server`, `game-core`, `frontend` — ok



## Roadmap проекта

### Этап 1. Играбельный PvP

Цель: довести базовый PvP-контур до состояния, в котором матч реально можно сыграть от начала до конца.

- Починить цикл фаз и хода в `game-core`.
- Реализовать рабочий `EndTurn`.
- Добавить валидацию владения картой и её `location`.
- Запретить невалидные действия на уровне `game-core`.
- Проверить, что два игрока могут завершить матч без рассинхрона state.
- Добить тесты `game-core` на фазы, ходы и невалидные сценарии.

Подзадачи этапа:

1. Нормализовать жизненный цикл матча в `game-core`.
- [ ] Зафиксировать ожидаемый порядок фаз: `RecoveryPhase -> DrawPhase -> ActionPhase -> EndPhase`.
- [x] Определить, в какой фазе матч стартует после создания сессии.
- [ ] Сделать так, чтобы переходы фаз происходили из реальных игровых действий, а не оставались ручными и недостижимыми.

2. Доделать механику завершения хода.
- [x] Реализовать `EndTurn` в `game-core`.
- [x] Проверить смену активного игрока.
- [x] Проверить сброс/обновление ресурсов на новый ход: фаза, action points, mana, добор, если он заложен правилами.
- [x] Убедиться, что ход нельзя завершить некорректным игроком.

3. Закрыть критичные валидации игровых действий.
- [x] Для `CastSpell`, `Summon`, `PlayCard` проверять владельца карты.
- [x] Проверять, что карта находится в допустимом `location` перед розыгрышем.
- [x] Проверять, что действие делает именно активный игрок, если это требуется правилами.
- [ ] Проверять, что после выполнения действия state остаётся консистентным.

4. Убрать явные дыры в PvP-логике.
- [x] Проверить, что нельзя сыграть карту соперника.
- [x] Проверить, что нельзя сыграть карту из колоды или discard pile.
- [x] Проверить, что нельзя ходить вне своей очереди.
- [x] Проверить, что матч не застревает после нескольких последовательных `EndTurn`.

5. Собрать минимальный end-to-end PvP-сценарий на уровне core/server-логики.
- [x] Создать матч на двух игроков.
- [x] Провести несколько валидных действий подряд.
- [x] Завершить несколько ходов подряд.
- [x] Проверить, что state у обоих игроков остаётся предсказуемым и одинаковым.

6. Расширить тестовое покрытие под выводы ревью.
- [x] Добавить тест на корректный старт матча и первую доступную фазу.
- [x] Добавить тесты на `EndTurn` и смену активного игрока.
- [x] Добавить тесты на запрет розыгрыша чужих карт.
- [x] Добавить тесты на запрет розыгрыша карт из неверного `location`.
- [x] Добавить тесты на невалидные действия вне своей очереди.

7. Зафиксировать Definition of Done для этапа.
- [x] `game-core` проходит `type-check`, тесты и сборку.
- [x] Базовый PvP-сценарий воспроизводится без ручных правок state.
- [ ] Нет известных критичных багов, блокирующих матч 1v1.

Критерий завершения этапа:
- PvP-матч создаётся, игроки ходят по очереди, матч не застревает и не ломает state.

### Этап 2. Серверная интеграция

Цель: поднять игру на сервере и обеспечить корректную серверную оркестрацию PvP-матча.

- Привязать WebSocket-соединение к конкретному `playerId`. [done]
- Запретить action от имени другого игрока. [done]
- Проверить создание сессии, вход игроков и доставку state через WebSocket.
- Убедиться, что сервер корректно маршрутизирует команды в `game-core`. [done на уровне `GameService` + тестов]
- Подготовить минимальный сценарий запуска игры на сервере. [done на уровне теста `server + game-core`, без реального клиента]

Критерий завершения этапа:
- PvP работает не только локально в коде, но и через серверный контур.

### Этап 3. Стабилизация Frontend + Backend

Цель: выровнять интеграцию фронта и бэка, поймать и исправить реальные баги после серверного запуска.

- Прогнать основные пользовательские сценарии `frontend <-> backend <-> game-core`.
- Ловить баги синхронизации state, валидации payload и UI-ошибки.
- Перевести `Simulation / Replay` с UI-заглушек на сервер / `game-core`.
- Починить frontend `lint` и падающие frontend-тесты.
- Обновить корневые `lint` / `test`, чтобы они покрывали все ключевые части проекта.
- Добавить серверные интеграционные тесты на два клиента и конфликтные сценарии.

Критерий завершения этапа:
- Основные пользовательские сценарии проходят стабильно, quality gates зелёные или близки к этому.

### Этап 4. Публичный серверный релиз

Цель: подготовить проект к нормальному размещению на сервере и использованию вне локальной среды.

- Проверить production-сборки и конфигурацию окружения.
- Подготовить серверный запуск и базовый smoke-check после деплоя.
- Убедиться, что логи, health-check и базовые debug-механизмы работают.
- Уточнить, какие части остаются demo/mock, а какие уже считаются рабочими.

Критерий завершения этапа:
- Проект разворачивается на сервере и проходит базовую проверку работоспособности.

### Этап 5. Админская версия

Цель: после стабилизации основного продукта начать развивать отдельный админский контур.

- Добавить админские эндпоинты для управления новостями.
- Перенести debug- и тестовые настройки в админскую часть.
- Продумать разграничение публичного и административного функционала.
- Определиться, как будет развиваться auth: demo/mock или полноценный backend-вариант.

Критерий завершения этапа:
- У проекта есть отдельный управляемый админский контур, не мешающий основной пользовательской версии.




### Что брать прямо сейчас

1. Закрыть оставшиеся дыры `Этапа 1`.
- [ ] Зафиксировать финальную модель фаз и решить, нужны ли реальные `RecoveryPhase` / `DrawPhase` уже сейчас.
- [ ] Добавить проверку консистентности state после ключевых действий.
- [ ] Решить, можно ли уже считать `Этап 1` завершённым или нужен ещё один PvP-сценарий с картами/уроном.

2. Дожать `Этап 2` до реального транспортного сценария.
- [ ] Проверить настоящий `WsGateway`, а не только `GameService`.
- [ ] Добавить тест или smoke-сценарий с двумя клиентами через WebSocket.
- [ ] Подтвердить доставку state обоим игрокам после действий.

3. Подготовить переход к интеграции `frontend <-> server`.
- [ ] Провести аудит текущего frontend-кода и понять, что мешает запуску PvP из UI.
- [ ] Составить список первых интеграционных багов/разрывов контрактов.
- [ ] Выбрать первый пользовательский сценарий, который будем доводить end-to-end.

Текущий выбранный пользовательский сценарий `v0`:
- Игрок 1 логинится, открывает `PlayPvpPage`, задаёт `sessionId` и при необходимости seed, затем нажимает подключение.
- Игрок 2 логинится, открывает `PlayPvpPage`, вводит тот же `sessionId` и подключается вторым участником.
- После `join` оба клиента получают `state`.
- Минимальное первое действие из UI — `EndTurn`.

Рекомендуемый порядок старта:
- Сначала добить реальный WebSocket-сценарий.
- Потом проверить связку `frontend <-> server`.
- Затем уже переходить к стабилизации UI и API-контрактов.

## Implementation Plan: PvP из UI

### Задача 1. Подготовить окружение для реального игрового контура
- [x] Добавить `server` в `docker-compose`.
- [x] Прокинуть порт игрового WebSocket-сервера в окружение.
- [x] Добавить frontend-конфиг для `VITE_WS_URL`.
- [x] Обновить README с актуальной схемой запуска `backend + server + frontend`.

### Задача 2. Определить bootstrap-контракт матча
- [x] Зафиксировать, откуда frontend получает `sessionId`.
- [x] Зафиксировать, как формируется `playerId` для подключения к матчу.
- [x] Решить, нужен ли временный mock/bootstrap через UI или отдельная HTTP-ручка.
- [x] Описать минимальный сценарий входа двух игроков в один матч.

Выбранный bootstrap-контракт `v0`:
- `sessionId` на первом этапе задаётся или генерируется прямо во frontend UI, без отдельной HTTP-ручки.
- `playerId` берётся из `authService.getSession().userId`.
- Если пользователь не авторизован, PvP-экран должен требовать вход или явно работать только в guest-режиме; по умолчанию считаем, что для PvP нужен логин.
- Первый игрок создаёт/вводит `sessionId` и подключается к WS как создатель матча.
- Второй игрок вводит тот же `sessionId` и подключается к той же WS-сессии.
- Для `join` используется текущее WS-сообщение: `{ type: 'join', sessionId, playerId, seed? }`.
- `seed` на первом этапе задаёт только первый игрок при создании матча; второй игрок подключается без собственного seed.
- Отдельная HTTP-ручка для lobby/matchmaking откладывается на следующий этап после первой живой интеграции UI.

### Задача 3. Сделать базовый WebSocket-клиент во frontend
- [x] Добавить отдельный frontend-сервис для подключения к игровому WS.
- [x] Реализовать `join`, получение `state`, отправку `action`.
- [x] Добавить базовую обработку ошибок соединения и разрыва связи.
- [x] Вынести формат клиентских/server-сообщений в типизированные frontend-контракты.

### Задача 4. Подключить `PlayPvpPage` к реальному серверу
- [x] Заменить текущую заглушку `PlayPvpPage` на рабочий экран подключения к матчу.
- [x] Добавить минимальную форму/кнопки для старта или входа в сессию.
- [x] После `join` показывать реальный матчевый state.
- [x] Добавить хотя бы одно реальное действие из UI, например `EndTurn`.

### Задача 5. Собрать минимальный пользовательский PvP-сценарий end-to-end
- [x] Открыть матч из UI первым игроком.
- [x] Подключить второго игрока.
- [x] Проверить получение state на обеих сторонах.
- [x] Проверить, что ход одного игрока отражается у второго.
- [x] Проверить, что `EndTurn` реально переключает активного игрока в UI.

Примечание:
- На текущем шаге это зафиксировано frontend smoke-тестами для двух пользовательских ролей (`создатель` и `второй игрок`) через fake WebSocket.
- Отдельный ручной прогон в двух реальных вкладках/браузерах остаётся полезным и может быть вынесен в следующий интеграционный проход.

### Задача 6. Закрыть первые интеграционные баги frontend <-> server
- [x] Исправить разрывы контрактов между UI и WS/server.
- [x] Добавить обработку невалидных payload и серверных ошибок в UI.
- [x] Устранить критичные проблемы синхронизации state.
- [x] Зафиксировать список известных ограничений первой рабочей версии PvP.

Известные ограничения первой рабочей версии PvP:
- `PvP` пока опирается на bootstrap `v0`: `sessionId` вводится вручную, lobby/matchmaking ещё нет.
- На фронте показывается обобщённый `raw state`; отдельный игровой UI по руке, столу, целям и эффектам ещё не собран.
- Текущий UX рассчитан на минимальный сценарий `join -> state -> EndTurn`; остальные боевые действия ещё не выведены в интерфейс.
- Автотесты UI используют fake `WebSocket`; ручной прогон в двух реальных вкладках всё ещё полезен как следующий шаг стабилизации.

### Задача 7. Подготовить базу для следующего этапа стабилизации
- [x] Решить, какие части PvP уже считаются рабочими, а какие остаются временными.
- [x] Обновить `FUTURE_TODO.md` по результатам первой живой интеграции.
- [x] Определить следующий фокус: UI-стабилизация, replay/sim, auth-связка или админский контур.

Решение по статусу `PvP v1`:
- Уже считаем рабочим:
- `game-core` больше не застревает на старте матча; ход и `EndTurn` работают стабильно.
- Базовые серверные ограничения есть: нельзя подменять `playerId`, нельзя зайти третьим игроком, нельзя подключаться к существующей сессии с конфликтующим `seed`.
- `PlayPvpPage` умеет создать/подключить матч, получить `state`, показать базовую сводку и отправить `EndTurn`.
- Есть автоматические проверки на `core`, `server` и `frontend`, включая smoke-сценарии для PvP UI и WebSocket-слоя.

- Остаётся временным:
- bootstrap `v0` с ручным вводом `sessionId` и без lobby/matchmaking.
- локальная browser-only auth, не связанная с полноценной серверной идентичностью.
- UI матча пока технический: нет нормального игрового поля, руки, существ, таргетов и боевых действий кроме `EndTurn`.
- нет ручного e2e-прогона в двух реальных вкладках как зафиксированного smoke-ритуала.
- replay/sim контур всё ещё живёт отдельно от полноценного PvP UX.

Следующий выбранный фокус:
- `Этап 3 — стабилизация frontend + backend/server`.
- Приоритет 1: довести реальный PvP UI от технического экрана до базового игрового интерфейса.
- Приоритет 2: зафиксировать ручной smoke-сценарий в двух вкладках и список обязательных регрессионных проверок.
- Приоритет 3: начать связывать auth/session слой с серверной игровой идентичностью.
- `Replay/Sim` и админский контур пока не приоритетнее стабилизации основного PvP-сценария.

Что брать следующим пакетом:
1. Собрать минимальное игровое поле на `PlayPvpPage`: активный игрок, локальный игрок, фаза, ход, список игроков, action log.
2. Добавить в UI ещё одно реальное действие кроме `EndTurn`, если серверный/core-контракт уже готов.
3. Описать и прогнать ручной smoke в двух реальных браузерных вкладках.
4. Решить, как будет выглядеть `auth -> player identity` без локального mock-only режима.

## План Подъёма Стенда

Цель:
- Поднять текущий проект на VPS с белым IP как `staging`-стенд для ручного тестирования, отлова интеграционных багов и дальнейшей стабилизации.

Этап A. Выбрать и подготовить VPS
- [ ] Выбрать VPS с минимально комфортными характеристиками: `2 vCPU`, `4 GB RAM`, `30-40 GB SSD`, `Ubuntu`, `белый IPv4`.
- [ ] Получить доступ по `SSH`.
- [ ] Обновить систему и создать рабочего пользователя.
- [ ] Установить `Docker` и `Docker Compose`.
- [ ] Настроить firewall и открыть базовые порты стенда.

Этап B. Подготовить staging-конфиг проекта
- [ ] Подготовить `.env` для staging-окружения.
- [ ] Определить внешний адрес для `frontend`, `backend` и `WS server`.
- [ ] Прописать корректные `VITE_API_URL` и `VITE_WS_URL` под внешний IP или домен.
- [ ] Проверить, что `docker-compose.yml` соответствует staging-сценарию.

Этап C. Развернуть проект на сервере
- [ ] Загрузить проект на VPS.
- [ ] Собрать контейнеры.
- [ ] Поднять `frontend + backend + server + postgres`.
- [ ] Проверить, что все сервисы стартуют без критичных ошибок.

Этап D. Провести базовый smoke-test стенда
- [ ] Открыть frontend по внешнему адресу.
- [ ] Проверить `backend health`.
- [ ] Проверить доступность `WebSocket`-сервера.
- [ ] Подключить двух игроков к одному матчу.
- [ ] Проверить `join`, получение `state` и `EndTurn` на реальном стенде.

Этап E. Зафиксировать результаты первой выкладки
- [ ] Собрать список багов, которые проявились только на реальном сервере.
- [ ] Разделить найденные проблемы на `critical / important / later`.
- [ ] Решить, что нужно исправить до следующего цикла тестирования.
- [ ] Обновить `FUTURE_TODO.md` по результатам стенда.

Принцип работы со стендом:
- На текущем этапе это именно `staging`, а не `production`.
- Основная цель стенда — реальное тестирование PvP и связки `frontend <-> server`, а не публичный релиз.
- После стабилизации можно отдельно решить, превращаем ли этот стенд в постоянный сервер или поднимаем отдельный production-контур.
